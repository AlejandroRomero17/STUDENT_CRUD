export default class StudentModel {
  constructor(id, name, lastname, grade, average) {
    this.id = id;
    this.name = name;
    this.lastname = lastname;
    this.grade = grade;
    this.average = average;
  }
}
